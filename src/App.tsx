import './App.css';
import Countdown from './components/countdown.component';

const App = () => {
	// return <Countdown startNumber={10} endNumber={20} />;
	return <Countdown startNumber={20} endNumber={10} />;
};

export default App;
